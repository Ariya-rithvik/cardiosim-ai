# ── CardioSim AI — Backend Environment Variables ──
# Copy this file to .env and fill in your values

# ── MedGemma Mode ──────────────────────────────
# true  = demo mode (instant, no GPU needed)
# false = real MedGemma 4B-IT inference (requires T4 GPU + ~6.5GB VRAM)
MEDGEMMA_MOCK=true

# ── Hugging Face Token ─────────────────────────
# Required only when MEDGEMMA_MOCK=false
# Get yours at: https://huggingface.co/settings/tokens
# The token needs read access to google/medgemma-4b-it
HF_TOKEN=your_hf_token_here

# ── MedGemma Model ID ──────────────────────────
# Default: google/medgemma-4b-it
# You can also use: google/medgemma-4b (base, not instruction-tuned)
MEDGEMMA_MODEL_ID=google/medgemma-4b-it

# ── Gemini Flash (for Explanations) ────────────
# Get yours at: https://aistudio.google.com/app/apikey
# If not set, explanations use mock text
GEMINI_API_KEY=your_gemini_api_key_here

# ── Server Settings ────────────────────────────
# PORT=8000
# HOST=0.0.0.0
